帮我使用 rust 框架 实现一个 上图所示的 web UI 界面，组件化

1.不需要实现圆角，依赖浏览器自己的实现
2.后续需要连接 aria2 rpc，请考虑
3.UI 中的相关配置需要存储到服务器本地，不是存储到浏览器中
4.单独css 不好维护 能不能切分到组件中 各个组件单独维护自身样式，统一样式再放到一个地方维护
5.不要将demo数据写入到界面中，使用mock API 模拟
6.使用 fontawsome 库来实现所有图标